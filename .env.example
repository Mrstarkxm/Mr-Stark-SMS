LAMIX_BASE_URL=https://panel.lamix.org/api/v1
LAMIX_API_TOKEN=
LAMIX_DEFAULT_OWNER_ADMIN_ID=1
LAMIX_TEST_PANEL_URL=https://panel.lamix.org/test-panel
# Real Lamix Test Panel uses the authenticated web session endpoint /api/test-numbers.
# Keep these local; never commit or share them.
LAMIX_SESSION_COOKIE=
LAMIX_CSRF_TOKEN=

# Vercel / scheduled sync
CRON_SECRET=replace-with-a-long-random-secret
